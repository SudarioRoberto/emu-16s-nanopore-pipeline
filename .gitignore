# Full dataset directories (too large for git)
data/raw_runs/
data/concatenated/
data/filtered/
data/trimmed/
data/emu_db/

# Keep example sample data tracked (override *.fastq.gz for example_sample/)
!data/example_sample/
!data/example_sample/**

# Results - keep combined and summary files
results/emu_per_sample/

# Logs - keep pipeline_log.tsv, skip verbose per-step logs
logs/*.out
logs/*.log
logs/*.json

# Conda
.conda/

# OS
.DS_Store
Thumbs.db
